# Kept deliberately small so the scheduled job installs fast and rarely breaks.
# lxml is intentionally absent: bs4's stdlib html.parser handles these pages, and
# lxml has no wheel for every Python version we might land on.
requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
pytest>=8.0

# Replays a real browser TLS fingerprint. Needed only for sources whose bot
# protection rejects Python's default handshake (NEW Co-op); everything else
# uses plain requests.
curl_cffi>=0.7

# Timezone database. Landus stamps its publish time in Central with no zone;
# bare Windows has no system tzdata.
tzdata>=2024.1
